# 2026-06-27 — Work log

Full day's log. Reconstructed from session memory after a sandbox reset wiped the original wiki location at `projects/trip-optimizer-.../knowledge/`. The wiki is now persisted in-repo at `docs/wiki/`.

## Open PRs at end of day

None (PR #69 merged at 14:45 EDT).

## Merges (newest first)

| # | Branch | Title |
|---|---|---|
| 69 | `fix/intro-pdf-only` | Make trip introduction PDF-only (remove on-screen card; keep headless gen for PDF). Merged 14:45 EDT as `c2bfb45`. |
| 68 | `fix/expert-apply-connection-drop` | Resume surgical Expert-Review apply via KV poll when the live stream drops |
| 67 | `feat/auto-introduction` | Auto-generate trip introduction as a separate post-build call |
| 66 | `fix/review-replan-load-failed` | full re-plan aborts mid-stream → honest errors, not bare "Load failed" |
| 65 | `feat/local-providers` | Local providers (verified drivers, guides, tours, wine tastings) |
| 64 | `fix/build-hang-output-reset` | preserve output selections across remounts; bound stalled build stream |

## Verification attempts on PR #69 (afternoon)

### Attempt 1 — 1:15 PM EDT — FAILED (browser agent loop)
- Target: commit preview https://10c11d94.trip-optimizer-6og.pages.dev
- Result: browser agent stuck on the wizard's date picker in Step 1 (Essentials). Brief text was entered but date widget never resolved cleanly; agent looped indefinitely.
- Outcome: zero coverage of either PR #69 verification item.

### Attempt 2 — 2:40 PM EDT — BLOCKED by preview env-var gap
- Same target preview URL, stronger browser model, stricter script.
- Result: agent successfully reached the build step. Build returned "Server missing `ANTHROPIC_API_KEY`" immediately — soft-fail firing.
- Outcome: surfaced a real infrastructure gap. Cloudflare Pages env vars are bound to Production only.

### User manual verify — 4:37 PM EDT — partial PASS, partial FAIL
- On https://www.routesmith.ai (production):
  - ✅ On-screen intro check: PASS. No card, panel, or paragraph above Day 1.
  - ❌ PDF intro check: FAIL. Downloaded PDF had no introduction section.
  - ⚠️ PDF generation slow (pre-existing, not a #69 regression).
- User clicked Download PDF immediately after Day 1 cards rendered. Race-condition diagnosis: headless `/api/introduction` call hadn't completed before PDF chain ran.

## Production URL change

User noted production URL is now https://www.routesmith.ai (custom domain on Cloudflare Pages). Original https://trip-optimizer-6og.pages.dev still works as alias.

## Wiki persistence incident

Initial wiki was written to `projects/trip-optimizer-8I7po52CRzyhB30QaOAMtA/knowledge/` (the documented per-project knowledge path). When user opened a new thread the wiki appeared empty — that location does not persist across thread/sandbox resets reliably. A later sandbox cleanup wiped the files entirely.

**Resolution:** moved the wiki into the repo at `docs/wiki/`, which persists via git. Filed engineering issue about the original location's broken persistence claim.

## Open infra work at end of day

1. Cloudflare Pages Preview env-var gap (see `concepts/known-issues.md` #1)
2. Wizard date picker automation friction (see `concepts/known-issues.md` #2)
3. Active PDF intro race condition (see `concepts/known-issues.md` #3 and `handoff.md`)
4. PDF generation perf (see `concepts/known-issues.md` #4)
