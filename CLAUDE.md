# Trip Optimizer — Agent / Contributor Notes

This file tells any AI agent (or contributor) working on this repo what
the non-negotiable behavior rules are. Read it before changing any code
that touches venue data, dates, prompts, or PDF export.

## VENUE VERIFICATION — HARD RULE

Never emit a venue, address, phone, opening hours, price, or day-of-week that has not been
verified against an external source or computed in code.

- Existence and business_status MUST be checked via Google Places (New) Place Details for every
  named venue before export. Venues that are CLOSED_PERMANENTLY, CLOSED_TEMPORARILY, or NOT_FOUND
  are BLOCKED and replaced — never flagged-and-shipped.
- Address, phone, hours, and website are OVERWRITTEN with Places values; on conflict, Places wins
  and the discrepancy is logged.
- Phone, exact address, precise hours, and price are verify-or-strip: if unconfirmed, omit and
  flag — never present as fact.
- Day-of-week, durations, time math, and sums are COMPUTED IN CODE, never generated by the model.
  (Anchor: Aug 25 2027 = Wednesday, Sep 5 2027 = Sunday.)
- Prose grounding (Perplexity / Reddit / local news) is for qualitative and sentiment context
  ONLY — never the basis for existence, status, address, phone, or hours.

This rule supersedes any instruction to "just generate" an itinerary. If a verification tool is
unavailable at runtime, fail safe: treat the venue as UNVERIFIED, not as operational.

## Implementation map

| Concern | File |
| --- | --- |
| Single-venue verify (Places (New) Text Search → Details, KV-cached) | `functions/api/places-verify.js` |
| Batch verify + flag taxonomy (called post-build from the client) | `functions/api/places-verify-batch.js` |
| Server-side verify inside `/api/find` (drops closed venues) | `functions/api/find.js` (`verifyVenuesForFind`) |
| Client-side merge + verify-or-strip | `src/placesVerify.js` |
| Computed weekday / date table injected into the build prompt | `src/dateFacts.js` |
| Pre-export gate before PDF render | `src/App.jsx` (PDF export path) |

## Flag taxonomy (authoritative)

Emitted by `/api/places-verify-batch` and `functions/api/find.js`,
consumed by the merge helper and the pre-export gate.

| code | severity | meaning |
| --- | --- | --- |
| `CLOSED_PERMANENTLY` | block | Places confirmed permanent closure |
| `CLOSED_TEMPORARILY` | block | Places confirmed temporary closure |
| `NOT_FOUND` | block | Places Text Search returned zero matches |
| `UNVERIFIED` | warn | Places lookup couldn't run (missing key / network / timeout) |
| `UNVERIFIED_SPECIFIC` | info | Client stripped phone / numbered-address / hours / booking_url from an UNVERIFIED venue |
| `CLOSED_ON_THIS_DAY` | warn | Venue verified open but closed all day on the item's scheduled weekday |
| `OUTSIDE_HOURS` | warn | Venue verified open but item's time is outside Places' posted hours |
| `WRONG_LOCATION` | block | Verified venue is too far (>50 km or per-leg widened radius) from any trip city — likely a wrong-city match |
| `PACING_IMPOSSIBLE` | block | Adjacent items can't be reached in the scheduled gap (travel time > gap) |
| `PACING_CONFLICT` | warn | Adjacent items have <15 min buffer after travel — tight but possible |

A venue with any `severity:"block"` flag must NOT reach the PDF
exporter. The pre-export gate is the last line of defense; it is not
optional, even when the user clicks "Export anyway."

## Environment

Required in Cloudflare Pages:

- `ANTHROPIC_API_KEY` (existing) — drives `/api/build` and `/api/find`
- `PERPLEXITY_API_KEY` (existing) — drives Sonar grounding + reservation confirmation
- `GOOGLE_PLACES_API_KEY` — **new**. Without it, every venue is UNVERIFIED. Add as encrypted secret in the Pages dashboard, never in code.
- `JOBS` KV binding (existing) — build job mirror
- `PLACES` KV binding — **new**. 30-day TTL per entry. Bind a fresh namespace in the Pages dashboard.

## Scope discipline

When touching this codebase:

- Read the existing code before editing. Trace the real call sites; do not assume.
- Change only what the task spec specifies. No unsolicited refactoring, renaming, or restyling.
- Never hardcode secrets. Keys come from `env.*` (Pages dashboard).
- Show diffs before deploying. Deploy is the maintainer's call.
- This codebase runs on Cloudflare Pages with Functions; the streaming `/api/build` endpoint
  is delicate (rewritten 2026-05-24 to fix a `waitUntil` cancellation bug) — do not
  add post-stream blocking work there. The post-build verification pass runs from the client.
