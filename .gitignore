node_modules
dist
# Output of scripts/publish-trip-site.mjs — generated per-trip site folders,
# not something to commit into this repo.
dist-sites/
.env
.env.local
.wrangler
# Visual regression captures (large binaries; see scripts/visual-baseline.md)
scripts/visual-baseline/
scratchpad/
