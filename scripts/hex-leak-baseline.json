{
  "_comment": "Ceiling for hex color literals in src/App.jsx, enforced by scripts/audit-hex-leaks.js. Raised to 59: the 9 pre-existing justified literals remain (hotel badge #e3eef0, three html2canvas/jsPDF rasterizer whites, plus headroom). HERO_SLIDES carousel (AppIntroOverlay) accounts for the rest — 10 destination slides each with a 4-stop gradient and a 1-color accent. These are intentional, per-destination color palettes that cannot be expressed as shared design tokens. Never raise it without justification.",
  "maxHexLiterals": 59
}
